version: 0.2

phases:
  install:
    runtime-versions:
      python: "3.11"
    commands:
      - pip install flake8 bandit pytest
      - rpm -ivh https://github.com/aquasecurity/trivy/releases/download/v0.49.1/trivy_0.49.1_Linux-64bit.rpm

  pre_build:
    commands:
      - cd analytics-service
      - pip install -r requirements.txt
      - echo "--- Unit Tests ---"
      - pytest test_app.py -v || true
      - echo "--- Linter (flake8) ---"
      - flake8 app.py --max-line-length=120 --ignore=E501,W503
      - echo "--- SAST (bandit) ---"
      - bandit -r app.py -ll -ii
      - echo "--- SCA (Trivy filesystem) ---"
      - trivy fs --exit-code 1 --severity CRITICAL --no-progress .
      - echo "--- ECR Login ---"
      - aws ecr get-login-password --region $AWS_DEFAULT_REGION | docker login --username AWS --password-stdin $AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com

  build:
    commands:
      - COMMIT_HASH=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c 1-7)
      - IMAGE_TAG=${COMMIT_HASH:=latest}
      - echo "--- Docker Build ---"
      - docker build -t $ECR_REPO:$IMAGE_TAG .
      - docker tag $ECR_REPO:$IMAGE_TAG $ECR_REPO:latest
      - echo "--- Container Scan (Trivy) ---"
      - trivy image --exit-code 1 --severity CRITICAL --no-progress $ECR_REPO:$IMAGE_TAG

  post_build:
    commands:
      - echo "--- Push to ECR ---"
      - docker push $ECR_REPO:$IMAGE_TAG
      - docker push $ECR_REPO:latest
      - echo "--- Update GitOps ---"
      - cd $CODEBUILD_SRC_DIR
      - GITHUB_TOKEN=$(aws ssm get-parameter --name /feature-flag/github-token --with-decryption --query Parameter.Value --output text)
      - sed -i "s|image:.*feature-flag-analytics-service:.*|image:\ $ECR_REPO:$IMAGE_TAG|" gitops/base/analytics-service.yaml
      - git config user.email "codebuild@ci.local"
      - git config user.name "CodeBuild"
      - git remote set-url origin https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPO}.git
      - git add gitops/base/analytics-service.yaml
      - git commit -m "ci(analytics-service)- update image to $IMAGE_TAG" || true
      - git push origin main || true

artifacts:
  files:
    - gitops/base/analytics-service.yaml
